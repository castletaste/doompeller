name: doom_geometry
description: Pure Dart Doom level geometry compiler producing packed GPU meshes.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  doom_wad:
    path: ../doom_wad
dev_dependencies:
  lints: 6.0.0
  test: 1.26.2
